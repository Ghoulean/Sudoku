/**
 * Contains validators. A validator defines a rule and fails a board if the rule is failed, and passes otherwise.
 * Validators can pass a board even if the board is not complete yet.
 */
package com.ghoulean.sudoku.validators;
